# osu-skin-skills 项目规则

本文件适用于本仓库全部数据库、脚本、测试和文档修改。

## 数据库变更报告

1. 修改数据库前先记录可比较的基线。默认以任务开始时的工作数据库为本次修改基线；需要汇总仓库全部未提交差异时，另以 Git `HEAD` 为基线，并明确说明采用哪个基线。
2. 每次数据库修改完成后，必须向用户提供完整、详细的数据库变更汇总，至少写明：
   - 表名；
   - 行的主键或可唯一定位该行的复合键；
   - 发生变化的字段；
   - 精确的修改前值和修改后值，包含 `NULL`、空字符串、布尔值和数值类型；
   - 新增或删除行时，给出完整行值；关联表必须给出新增或删除的完整复合键。
3. 多行具有完全相同的字段变化时可以合并报告，但必须列出每一个受影响的主键，不能用“等”“相关记录”或只给数量代替。
4. 汇总必须覆盖事务产生的全部表差异，包括标签定义、元素标签关联、主表备注、客户端范围和专属详情表；不能只汇总用户最先点名的字段。
5. 同时存在用户原有修改或已暂存修改时，不得把它们冒充为本次修改。报告中区分“本次基线后的变化”和“相对 Git HEAD 的工作区总差异”。

## lazer 行为的持久化表述

1. 数据库和仓库文档中关于 lazer 的内容只记录用户可观察的实际行为，不写具体源码实现。
2. 不得在数据库 `description`、`notes`、标签定义或 reference 文档中写入：
   - commit hash；
   - C# 类名、方法名或源码文件路径；
   - 内部资源路径、管理器名称或调用链；
   - “某行源码调用某方法”一类实现说明。
3. 可以为了核验而阅读和引用 lazer 源码，但源码证据只放在当次调查记录或给用户的核验说明中，不写入数据库和长期文档。
4. 行为描述应直接回答：lazer 是否读取 legacy 皮肤资源、在哪个模式或界面生效、缺失时如何回退，以及是否仅导入/保存但不实际消费。

## 消费者核验标准

1. `elements.client` 按实际消费者标注：
   - `both`：stable 与 lazer 都会实际读取该用户皮肤资源；
   - `stable`：lazer 没有 legacy 皮肤消费点；
   - `lazer`：仅 lazer 实际读取。
2. 能解析、导入、导出、保留文件，或客户端内置资源存在同名文件，都不能证明用户 legacy 皮肤资源会生效。
3. 将记录标为 `both` 前必须核对真实消费点。检查直接名称、动态拼接名称、模式 transformer、配置自定义路径和 fallback，不能只依赖全仓库一次关键词搜索。
4. 通用资源与模式专用资源必须逐项核对，不能互相类推。例如通用 `hit300g` 与 `mania-hit300g`、通用 `hit300k` 与 `taiko-hit300k` 是不同消费者。
5. 文件存在、动画帧选择、游玩渲染、结算界面使用和导入保留是不同事实，必须分开建模和描述。